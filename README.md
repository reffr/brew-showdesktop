# Reffr Brew-showdesktop

## How do I install these formulae?

`brew install reffr/brew-showdesktop/<formula>`

Or `brew tap reffr/brew-showdesktop` and then `brew install <formula>`.

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
